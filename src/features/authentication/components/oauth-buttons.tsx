"use client";

import * as React from "react";
import { GoogleButton } from "./google-button";

export const OAuthButtons: React.FC = () => {
  return <GoogleButton label="Continue with Google" />;
};
